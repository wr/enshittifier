#!/bin/sh
# Run the engine's pytest suite before pushing. Blocks the push on failure
# so broken tests don't reach the remote (and therefore can't reach a PR).
# Activate once: `git config core.hooksPath .githooks`
exec scripts/run-tests.sh
