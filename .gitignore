.venv/
__pycache__/
*.pyc
*.pyo
.pytest_cache/

# macOS Finder cruft
.DS_Store

# Stray build artifacts (the legacy Tk/py2app installer was removed,
# but these directory patterns can still appear from local experimentation).
build/
dist/
Enshittifier.dmg

# SwiftPM / Xcode
.build/
.swiftpm/
# Enshittifier.xcodeproj is generated by xcodegen from project.yml, but is
# committed so cloning + opening in Xcode works without installing xcodegen.
# Only ignore the user-specific bits.
*.xcodeproj/project.xcworkspace/xcuserdata/
*.xcodeproj/xcuserdata/
*.xcuserstate
installer-swift/build/
Enshittifier-native.dmg
Enshittifier-*.dmg
